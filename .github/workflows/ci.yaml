name: CI

on:
  push:
    branches: 
      - "**"
  pull_request:
    branches:
      - '**'

permissions:
  contents: read

jobs:
  build:
    timeout-minutes: 20

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 16.x

      - run: npm ci

      - run: npm run lint

      - run: npm run build
